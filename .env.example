# Optional: add the production GA4 measurement ID before deployment.
# The site works without it; when present, page views and SEO conversion events
# are sent through the shared SiteLayout tracking layer.
PUBLIC_GA4_ID=G-XXXXXXXXXX
